#include<stdio.h>
int main()
{
        int i,j,no;
        for(i=1;i<=5;i++)
        {
                for(j=1;j<=i;j++)
                {
                        no++;
                printf("%d ",no);
                }
                printf("\n");
        }
return 0;
}
